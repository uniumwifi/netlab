Initial development setup:
$ virtualenv ~/netlab
$ source ~/netlab/bin/activate
$ pip install bottle
$ pip install requests

Daily development work:
$ source ~/netlab/bin/activate

Run the daemon
$ make netmgr 

Install all
$ make
